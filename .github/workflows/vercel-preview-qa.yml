name: CineLoom Vercel Preview QA

on:
  pull_request:
  workflow_dispatch:

jobs:
  preview-qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm run smoke
      - run: npm run high-priority-check
      - run: npm run final-launch-check
